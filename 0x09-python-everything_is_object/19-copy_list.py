#!/usr/bin/python3
"""the copy_list function
"""


def copy_list(l):
    """function that copy a list

    Args:
        l (list): the list to copy

    Returns:
        list: the new list
    """
    new = l.copy()
    return new
